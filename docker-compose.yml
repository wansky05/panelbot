version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: express
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
